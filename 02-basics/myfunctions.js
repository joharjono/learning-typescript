//any
function calNumber(num: number) {
    // num.toUpperCase(); // but when s someone add this. is the problem
    // return num + 3;
    return "hello";
}
function getUpper(val) {
    return val.toUpperCase();
}
function singUpUser(name, email, isPaid) { }

var loginUser = function (name, email, isPaid) {
    if (isPaid === void 0) { isPaid = false; }
};
let myValue = calNumber(5); // not problem
// getUpper(5);
singUpUser("jo", "jo@gmail.com", false);
loginUser("nikolas", "nikolas@gmail.com");

